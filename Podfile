# Uncomment the next line to define a global platform for your project
platform :ios, '10.3'

target 'Spreedly' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Spreedly
  pod 'Adyen3DS2'
  pod 'SwiftyJSON', '~> 4.0'

  target 'SpreedlyTests' do
    inherit! :complete
    # Pods for testing
  end

end
